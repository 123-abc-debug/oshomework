# 课程要求
24.学生成绩管理系统
现有学生成绩信息文件1（1.txt），内容如下
姓名 学号 语文 数学 英语 
张明明 01 67 78 82
李成友 02 78 91 88
张辉灿 03 68 82 56
王露 04 56 45 77
陈东明 05 67 38 47
…. .. .. .. …
学生成绩信息文件2（2.txt）,内容如下:
姓名 学号 语文 数学 英语 
陈果 31 57 68 82
李华明 32 88 90 68
张明东 33 48 42 56
李明国 34 50 45 87
陈道亮 35 47 58 77
…. .. .. .. …
试编写一管理系统,要求如下:
## 1)实现对两个文件数据进行合并,生成新文件3.txt
第一步，实现数据的加载
使用buffered来拆解代码 按行来读取文件
接着就是循环读取每一行，每次读取的时候先用任意数量的空白字符（空格 / Tab）进行分割。
每行数据会被拆成数组，接着从后往前解析成绩和学号，这里考虑到名字有几个字组成，所以parts.length - 4 之前的所有字段都是名字。
最后创建一个个学生对象，把名字 + 学号 + 三科成绩封装成一个 Student 对象，然后加入 students 列表。


第二步，合并文件的思路
这里就是loadform两次 这样就是两次把内容放到students这个数组里面，它本质是利用 “同一个 students 列表” 来存储两个文件的内容。

第三步，把所有学生写入一个新文件
bw.newLine();这样表头写完后游标移动到下一行，方便逐行写学生记录。
bw.write(student.toString());

作用：将当前 student 对象的字符串表示写入缓冲区。

依赖：这依赖于你在 Student 类中重写（override）了 toString() 方法，且 toString() 返回的格式应当与文件列顺序一致（例如 name + "\t" + id + "\t" + chinese + "\t" + math + "\t" + english）。

如果没有重写 toString()：默认的 Object.toString() 会输出类似 com.example.Student@1a2b3c 的类名+哈希值，这显然不是需要的文本数据。





2)抽取出三科成绩中有补考的学生并保存在一个新文件4.txt
1.1 首先，要找到补考的学生，这里思路是先遍历students里面的学生信息，判断student是否成绩不及格

这里使用的是 public boolean hasFailed() {
        return chinese < 60 || math < 60 || english < 60;
    }

这里先是使用一个过滤不合格学生的函数，在一个failed的数组里面，要是有任意一门不合格的，就把学生加到这个数组里面


1.2 接着就是把不及格的人的代码打印到txt.4里面
首先，还是读文档的基本操作，写个表头
接着，是把failedsubject里面再加一个属性，就是failedsubject,用join加一个不及格的属性
这里的getsubject 里面自带了学科加成绩




3)合并后的文件3.txt中的数据按总分降序排序(至少采用两种排序方法实现)

首先，我觉得这里也应该再创建一个文件5.txt，名字，里面再多打印一个total score的属性,然后你只用降序的方式来实现排序，并且要两种来实现



4)输入一个学生姓名后,能查找到此学生的信息并输出结果(至少采用两种查找方法实现)
这里我使用两种查找方式来实现

代码这里实现的差不多了，各个任务都完成了


5)要求使用结构体,链或数组等实现上述要求.
6)采用多种方法且算法正确者,可适当加分.







